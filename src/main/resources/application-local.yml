spring:
  config:
    activate:
      on-profile: local

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB

  datasource:
    hikari:
      jdbc-url: jdbc:h2:mem:retripdb;DB_CLOSE_DELAY=-1
      username: sa
      password:

  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        dialect: org.hibernate.dialect.H2Dialect
        hbm2ddl:
          auto: create

  h2:
    console:
      enabled: true

  cloud:
    gcp:
      storage:
        bucket: bucket
        project-id: project

  ai:
    google:
      genai:
        api-key: AIzaSyDuG-FdXYOYAPQIyLgSLFKqhvcap2pkkvM
        project-id: gen-lang-client-0358830517
        chat:
          options:
            model: gemini-2.5-flash
            response-mime-type: application/json


logging:
  level:
    com.vibetrip: debug

jwt:
  secret-key: dGVzdHRlc3R0ZXN0dGVzdHRlc3R0ZXN0dGVzdHRlc3R0ZXN0dGVzdA==

app:
  cache:
    configs:
      appleKeys:
        ttl: 24h
        max-size: 10

ai:
  suno:
    api-key: 18aa1206d9761a22b9e1bc4b325ce9bf
    callback-uri: http://58.29.34.29:8080/callback