spring:
  config:
    activate:
      on-profile: local
    import: aws-secretsmanager:local/retrip

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB

  datasource:
    hikari:
      jdbc-url: jdbc:h2:mem:retripdb;DB_CLOSE_DELAY=-1
      username: sa
      password:

  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        dialect: org.hibernate.dialect.H2Dialect
        hbm2ddl:
          auto: create

  h2:
    console:
      enabled: true

  cloud:
    gcp:
      storage:
        bucket: bucket
        project-id: project

  ai:
    google:
      genai:
        api-key: ${GEMINI_API_KEY}
        project-id: ${GEMINI_PROJECT_ID}
        chat:
          options:
            model: ${GEMINI_MODEL}
            response-mime-type: ${GEMINI_RESPONSE_TYPE}

logging:
  level:
    com.vibetrip: debug

jwt:
  secret-key: ${JWT_SECRET_KEY}

app:
  cache:
    configs:
      appleKeys:
        ttl: 24h
        max-size: 10

ai:
  suno:
    api-key: ${SUNO_API_KEY}
    callback-uri: ${SUNO_CALLBACK_URL}

firebase:
  key: ${FIREBASE_KEY}
